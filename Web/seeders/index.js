const users = require('./users');
const cookingmethods = require('./cookingmethods');
const foodgroups = require('./foodgroups');
const foods = require('./foods');
const tables = require('./tables');

module.exports = (count)=>{
    // users(count*3)
    // cookingmethods()
    // foodgroups()
    // foods()
    // tables(count)
}